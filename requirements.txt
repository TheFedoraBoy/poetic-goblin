# Core
Flask>=3.0

# WSGI server (production)
gunicorn>=22.0

# Dev (SQLite built-in, no extra deps needed)
# Just run: pip install Flask

# ─── Production Database ─────────────────────────────
# MySQL
PyMySQL>=1.1

# PostgreSQL
psycopg2-binary>=2.9

# ─── Production Services ─────────────────────────────
# Rate limiting
Flask-Limiter>=3.0

# S3 file storage (also works with Cloudflare R2)
boto3>=1.34

# Error monitoring
sentry-sdk[flask]>=2.0

# Redis (for rate limit storage and session storage in production)
# redis>=5.0
